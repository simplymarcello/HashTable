Created by Marcello Martins on Friday, October 30, 2015

/** @file README.txt */

i. All parts were completed. 

ii. There are no bugs that i know of.

iii. To run this program simply unzip and type "make all" in a terminal window within the same dir as the files. This program should run using your selected makes, run1linear, run1quadratic, run1double, run2short and run2.

iv. This program doesn't write to any files and only diplays the output on screen.

The exact R I am using is a function which returns the largest, previous prime compared to the table size. e.g. if table size is 101 R = 97.